#include <iostream>
#include "mpi.h"
#include <bits/stdc++.h>

#ifndef GEN_NUM
#define GEN_NUM

void generate_matrix(std::vector<std::vector<double>> &A);

void generate_vector(std::vector<double> &b);

#endif